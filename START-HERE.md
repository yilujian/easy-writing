# 易创 · 本地增强版

这是在 yilujian/easy-writing 原项目上修改的可运行本地版，保留原有工作台，不是界面演示。

## 直接使用（推荐）

1. 将整个压缩包解压到一个可写入的固定文件夹。不要在压缩包中直接运行。
2. 安装 Node.js 22 或更高版本：https://nodejs.org 。安装过的可用 `node --version` 检查。
3. Windows：双击 `start-windows.bat`。macOS：运行 `start-macos.command`；若没有执行权限，在终端执行 `sh start-macos.command`。Linux：执行 `sh start-linux.sh`。
4. 浏览器会打开 http://127.0.0.1:6789 。如果没有自动打开，手动输入此地址。保持启动窗口运行。
5. 在「我的作品」新建作品，进入编辑器写作。基础写作无需登录、无需 API Key，也无需联网。

发行包已包含 dist 构建产物；以上启动不需要 pnpm、Rust 或下载项目依赖。
这不是 Windows EXE 安装包；它是 Node.js 本地服务 + 浏览器工作台。

## 本次增加了什么

- **本地启动器**：Windows / macOS / Linux 启动脚本；只监听 127.0.0.1，固定端口 6789。
- **本地中心**：侧栏查看连接状态、备份时间、备份位置、快照列表，手动备份、下载与恢复。
- **浏览器作品硬盘快照**：启动约 15 秒后第一次检查，之后每 5 分钟检查；有变化时保存，保留最近 30 份。
- **模型本机转发**：沿用原来的模型管理与 AI 界面，由本机 Node 服务请求用户配置的模型，解决浏览器跨域问题；支持流式输出与中止。
- **全局搜索**：把原本仅桌面端启用的 Ctrl+K / ⌘K 作品与功能搜索开放到浏览器版，并加入本地中心入口。
- **依赖兼容**：迁移 pnpm 11 的构建脚本白名单格式，维持原项目允许的三个构建依赖。

## 数据在哪里，如何恢复

即时写作仍然保存在当前浏览器的 IndexedDB / localStorage；新增的硬盘快照在本文件夹的 `local-data/backups`。
在本地中心会显示实际绝对路径。快照通过临时文件写入再重命名，完整写入后才轮换旧快照。

硬盘快照包括：未删除作品、分卷目录、章节正文，以及大纲、角色、设定、时间线、故事线。
**不包括**：API Key、模型配置、聊天记录、历史版本、灵感素材、回收站、工作流运行断点和整套应用设置。它是作品快照，不是整机数据库镜像。

恢复方法：

- 同一台电脑：本地中心 → 某份快照「查看与恢复」→ 选择作品 →「恢复为新作品」。
- 换电脑：复制整个应用文件夹或带走快照 JSON，启动后在本地中心选择「从文件查看与恢复」。
- 单本原版 JSON / TXT：仍在「我的作品」中导入。

恢复使用原项目导入流程，生成新的作品和实体 ID，不覆盖现有作品；正文按文本恢复，富文本排版和工作流运行状态不恢复。快照原始 JSON 中保留导出时的正文 contentJson，供后续开发使用。

页面关闭后不会继续定时备份。退出前建议在「本地中心」点一次「立即备份」，看到成功再关闭。
固定使用 **127.0.0.1:6789**、同一浏览器和同一浏览器用户配置；localhost、不同端口和不同浏览器会形成独立的浏览器数据区。如果看不到旧作品，请先检查打开的地址，再从快照恢复。

## 配置 AI

打开「模型管理」添加模型，填写提供商的 OpenAI 兼容 Base URL、实际模型名称和自己的 API Key，再测试连接并设为默认。

本机模型常用地址：

| 工具 | 常用 Base URL | API Key |
| --- | --- | --- |
| Ollama | `http://127.0.0.1:11434/v1` | 通常留空 |
| LM Studio | `http://127.0.0.1:1234/v1` | 按本机服务设置 |
| 其他兼容服务 | 服务商提供的完整基础地址 | 按服务商要求 |

本地模型需要自行安装、下载并启动，包内不包含模型。实际地址、端口和模型名称以你机器的服务配置为准。
使用外部服务时，调用中的正文及密钥会从本机发往你配置的服务商。转发器不保存 API Key 或请求正文日志；密钥仍由原项目保存在本机浏览器配置中。

## 源码学习入口

先读 `docs/LOCAL-DEVELOPMENT.md`，再阅读原版 `README.md`。原项目源码和 Tauri 桌面端源码全部保留在包内。

需要继续修改时（构建过程需联网安装依赖）：

```sh
npm install -g pnpm@11
pnpm install --frozen-lockfile
pnpm build
pnpm start:local
```

开发预览 `pnpm dev` 沿用原 Vite 浏览器模式，它不包含 Node 硬盘备份与模型代理。验证本地增强能力应使用 `pnpm build` 后 `pnpm start:local`。

可选环境变量：`EW_PORT` 改端口；`EW_DATA_DIR` 改硬盘快照根目录。一般不建议改端口，以免浏览器数据区变化。
榜单爬虫与桌面系统窗口等原本依赖 Tauri 的功能，仍需自行构建桌面版；浏览器增强版没有移植这些系统能力。

## 已验证与未验证

- 已通过 TypeScript 检查与生产构建。
- 已通过本地服务自动化测试：备份读写、去重、无效输入、30 份保留、安全边界、SPA 路由、AI 流式代理和供应商错误状态。
- 已通过 IndexedDB 集成测试：创建作品、保存正文、导出、恢复副本及原稿保留。
- AI 转发使用模拟服务验证；未使用真实付费密钥，也未实测你本机的 Ollama / LM Studio。
- 此环境未进行浏览器界面验收、Windows / macOS 实机启动或 Tauri 安装包构建。

## 来源与许可

上游：https://github.com/yilujian/easy-writing

基于提交 `74ebfe496f792dba61f2001581023bee766ef89d`（原版本 1.0.6）。
原作者：yilujian。本次是非官方本地增强派生版本。保留原 LICENSE，新增及修改代码同样按 AGPL-3.0-only 提供。完整来源与修改说明见 `LOCAL-NOTICE.md`。
